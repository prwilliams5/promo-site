import { Box } from '@chakra-ui/react'
import styled from '@emotion/styled'

export const ExpSection = styled(Box)`
  padding-left: 3.4em;
  text-indent: -3.4em;
`

export const ExpDate = styled(Box)`
  font-weight: bold;
  margin-right: 1em;
`
